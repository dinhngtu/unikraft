$(eval $(call addlib_s,libukbus,$(CONFIG_LIBUKBUS),y))

CINCLUDES-$(CONFIG_LIBUKBUS)		+= -I$(LIBUKBUS_BASE)/include
CXXINCLUDES-$(CONFIG_LIBUKBUS)		+= -I$(LIBUKBUS_BASE)/include

LIBUKBUS_SRCS-$(CONFIG_LIBUKBUS) += $(LIBUKBUS_BASE)/bus.c
